@inherits VGCarouselBase

@if (Images?.Count > 0)
{
    <section class="carousel" aria-label="@AriaLabel">
        <ol class="carousel__viewport">
            @for (int i = 0; i < Images.Count; i++)
            {
                var img = Images[i];
                var prevIndex = (i == 0) ? Images.Count - 1 : i - 1;
                var nextIndex = (i == Images.Count - 1) ? 0 : i + 1;

                <li id="carousel__slide@(i + 1)"
                    tabindex="0"
                    class="carousel__slide">
                    <img src="@img.ImageUrl"
                         alt="@img.Caption"
                         class="carousel__image" />

                    <div class="carousel__snapper">
                        <a class="carousel__prev" @onclick="@(() => ScrollToSlide(prevIndex + 1))">‹</a>
                        <a class="carousel__next" @onclick="@(() => ScrollToSlide(nextIndex + 1))">›</a>
                    </div>
                </li>
            }
        </ol>

        <aside class="carousel__navigation">
            <ol class="carousel__navigation-list">
                @for (int i = 0; i < Images.Count; i++)
                {
                    <li class="carousel__navigation-item">
                        <a class="carousel__navigation-button" @onclick="@(() => ScrollToSlide(i + 1))"></a>
                    </li>
                }
            </ol>
        </aside>
    </section>
}